import 'package:flutter/material.dart';

class CreateListigPg extends StatelessWidget {
  const CreateListigPg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("CreateListigPg"),
      ),
    );
  }
}
