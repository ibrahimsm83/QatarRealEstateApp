import 'package:flutter/material.dart';

class ContactUsPg extends StatelessWidget {
  const ContactUsPg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("ContactUsPg"),
      ),
    );
  }
}
