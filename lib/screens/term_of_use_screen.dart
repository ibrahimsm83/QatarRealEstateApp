import 'package:flutter/material.dart';

class TermOfUsePg extends StatelessWidget {
  const TermOfUsePg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("TermOfUsePg"),
      ),
    );
  }
}
