import 'package:flutter/material.dart';

class BlogPg extends StatelessWidget {
  const BlogPg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("blog_screenPg"),
      ),
    );
  }
}
