import 'package:flutter/material.dart';

class SellPg extends StatelessWidget {
  const SellPg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("SellPg"),
      ),
    );
  }
}
